apiVersion: v1
kind: Service
metadata:
  name: {{ include "forge-chart.fullname" . }}
  labels:
    {{- include "forge-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "forge-chart.selectorLabels" . | nindent 4 }}
